# ~/.xprofile
# Contains programs to be run when Xorg is started. 
# I'm a pathological WM/DE hopper, so I moved `exec asdf` to ~/.xinitrc
# Remember to add `[ -f ~/.xprofile ] && . ~/.xprofile` to xinitrc

# Disable ThinkPad touchpad
xinput --disable "$(xinput --list | grep Synaptics | cut -d' ' -f6 | tr -d 'id=')" &

# Scroll with ThinkPad middle button in OpenBSD
xinput --set-prop "/dev/wsmouse" "WS Pointer Wheel Emulation" 1
xinput --set-prop "/dev/wsmouse" "WS Pointer Wheel Emulation Button" 2
xinput --set-prop "/dev/wsmouse" "WS Pointer Wheel Emulation Axes" 6 7 4 5

# Start some programs
picom &
dunst &
unclutter --timeout 3 &
sxhkd &
batdaemon &
redshift &

# Set wallpaper
feh --no-fehbg --bg-fill \
    ~/images/wallpapers/"$(find ~/images/wallpapers -name "*" | shuf | head -1)" || \
    xsetroot -solid "#999999" &

# Load Xresources
export X_RES_COLOR="gruvbox-dark" && \
    xrdb -merge -I"$XDG_CONFIG_HOME"/xresources "$XDG_CONFIG_HOME"/xresources/Xresources

# Customize keyboard
setxkbmap -option caps:escape
setxkbmap -option compose:ralt
