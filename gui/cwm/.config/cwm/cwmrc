# More i3-like workspace behavior
sticky yes

# Prevent conflict with Org-Mode
unbind-key M-Return
unbind-key M-Right
unbind-key M-Left
unbind-key M-Down
unbind-key M-Up

# Remap those keys
bind-key MS-Return window-hide
bind-key CM-Right group-cycle
bind-key CM-Left group-rcycle
bind-key CM-Down window-lower
bind-key CM-Up window-raise

# Some pretty stuff
borderwidth 4
color activeborder Crimson
color inactiveborder Black
